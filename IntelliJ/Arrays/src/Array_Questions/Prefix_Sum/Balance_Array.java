package Array_Questions.Prefix_Sum;

import java.util.ArrayList;

public class Balance_Array {
    public static void main(String[] args) {

    }

    public static int solve(ArrayList<Integer> A) {
        //4 prefixSums
        //prefix : even and Odd separately
        //suffixSum : even and odd separately

        int[] evenPrefixSum = new int[A.size()];
        int[] oddPrefixSum = new int[A.size()];
        int[] evenSuffixSum = new int[A.size()];
        int[] oddSuffixSum = new int[A.size()];

        //calculate prefixSum for even index elements
//        for(int i = 0; i < )
        return -1;
    }
}
